# Upstream digest watch
#
# Standard Notes doesn't publish Semver-tagged Docker images for `server` —
# only `:latest` and commit-SHAs. This file tracks the current digest of
# the upstream `:latest` tag so Renovate can notify us via a PR whenever
# upstream pushes a new image.
#
# The Unraid CA template (templates/standardnotes-server.xml) intentionally
# stays on `:latest` so users get automatic updates via Unraid's
# "Force Update" button — but PRs here let us audit upstream changes and
# pin downwards if something breaks.
#
# Format: <image>@<digest>   (one per line, Renovate reads these via the
# custom manager defined in renovate.json)

standardnotes/server@sha256:6b371bc0c3ae755500b82f4c580fae7fd768e5f9214cdc2f195fc130cf6ffdcd
